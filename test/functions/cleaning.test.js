describe("The function cleaning up old auctions", function () {
  describe("triggered once a week", function () {
    it(
      "removes all of the auction data for the auctions that are 8 or more days old"
    )
  })
})
